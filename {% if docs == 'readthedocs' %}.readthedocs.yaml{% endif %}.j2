version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"  # see pyproject.toml

{%- if readthedocs_tool == 'mkdocs' %}
mkdocs:
  configuration: mkdocs.yml
{%- elif readthedocs_tool == 'sphinx' %}
sphinx:
  configuration: docs/sphinx/conf.py
{%- endif %}

python:
  install:
    - requirements: docs/{{ readthedocs_tool }}/requirements.txt
